import { CheckEqualInputWithDirective } from './check-equal-input-with.directive';

describe('CheckEqualInputWithDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckEqualInputWithDirective();
    expect(directive).toBeTruthy();
  });
});
