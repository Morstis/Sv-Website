import { XssDirective } from './xss.directive';

describe('XssDirective', () => {
  it('should create an instance', () => {
    const directive = new XssDirective();
    expect(directive).toBeTruthy();
  });
});
